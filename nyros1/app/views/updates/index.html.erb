<h1>Attendance</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Present</th>
    <th>Late</th>
    <th>Absent</th>
  
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @updates.each do |update| %>
  <tr>
    <td><%= update.date %></td>
    <td><%= update.present %></td>
    <td><%= update.late %></td>
    <td><%= update.absent %></td>

    <td><%= link_to 'Show', update %></td>
    <td><%= link_to 'Destroy', update, :confirm => 'Are you sure?', :method => :delete %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Today attendance', new_update_path %>
<%= link_to "Employee list", registers_path %>
